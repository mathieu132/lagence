{% extends 'base.html.twig' %}

{% block title %}Hello LieuController!{% endblock %}

{% block body %}

<table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Adresse</th>
                <th>Prix</th>
                <th>Surface</th>
                <th>Capacité</th>
                <th>Type</th>
            </tr>
        </thead>
        <tbody>
        {% for lieu in lieux %}
            <tr>
                <td>{{ lieu.id }}</td>
                <td>{{ lieu.adresse }}</td>
                <td>{{ lieu.prix }}</td>
                <td>{{ lieu.surface }}</td>
                <td>{{ lieu.capacite }}</td>
                <td>{{ lieu.type }}</td>
                <td>
                    {# <a href="{{ path('lieu_show', {'id': lieu.id}) }}">Voir</a>
                    <a href="{{ path('lieu_edit', {'id': lieu.id}) }}">Modifier</a> #}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
